# Backlog

Items tracked in `bl` (backlog CLI). This file is an index only.

## Medium Priority

- **BL-292** — Wire up cloud maintenance (archive + prune) into `granicus gc`
- **BL-293** — Unify scheduling behind CLI/API with mode-based routing
- **BL-296** — Adaptive resource-aware concurrency: replace manual pools with auto-managed rate limiting

## Low Priority

(none)

## Resolved

- **BL-294** — Rename `source` package to `registry` (three-layer naming, ADR-007)
- **BL-295** — Rename Connection to Resource throughout codebase
